# Copy this to .env and fill in your values
# For local dev, the Vite proxy handles /api → localhost:8080 automatically

# Override the API base URL if not using the Vite proxy (e.g., direct deployment)
# VITE_API_URL=https://api.yourdomain.com/api

# App metadata
VITE_APP_NAME=TradePro
VITE_APP_VERSION=1.0.0
